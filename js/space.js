export var space;
(function (space) {
    space.size = 100;
    function init() {
        starts();
    }
    space.init = init;
    function tick() {
    }
    space.tick = tick;
    function starts() {
    }
})(space || (space = {}));
export default space;
